const DataAdmin = () => {
    return(
        <h1>
            
            {/* exports.CreateKamar = async(req, res) => {
  const {type_kamar, fasilitas_kamar, id_kasurKamar, deskripsi_kamar, status_kamar, image_kamar, url_image} = req.body
  try {
    const response = await kamar2.create({
      type_kamar, fasilitas_kamar, id_kasurKamar, deskripsi_kamar, status_kamar, image_kamar, url_image
    })
    res.status(201).json({msg: "user created", response})
  } catch (error) {
    res.status(400).json({ msg: error.message });
  }
} */}
        </h1>
    )
}